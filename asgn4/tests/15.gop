package main

func main() {
    var c,d,e int

    // changed all ops to yield booleans
    if c*d != 0 {
        d = d + 1
    } else if (d & e) != 0 {
        e = e + 1
    } else {
        d = d - 1
    }
}

// A label with "NOTYPE" in generated, like, 301 NOTYPE
// pls check other stuff also