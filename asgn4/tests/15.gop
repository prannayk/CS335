package main

func main() {
    var c,d,e int

    // changed all ops to yield booleans
    if c != 0 && d != 0 {
        d = d + 1
    } else if (d & e) != 0 {
        e = e + 1
    } else {
        d = d - 1
    }
}
